# Human Pages API
API_URL=https://humanpages.ai

# Agent credentials (leave blank to auto-register on first run)
AGENT_API_KEY=

# Agent registration info (used only if AGENT_API_KEY is blank)
AGENT_NAME=Errand Bot
AGENT_DESCRIPTION=Hires nearby humans for physical-world tasks that AI cannot do alone
AGENT_CONTACT_EMAIL=bot@example.com

# Webhook server (optional — the bot works without webhooks by polling the API)
# WEBHOOK_PORT=4000
# WEBHOOK_HOST=0.0.0.0
# WEBHOOK_URL=https://your-public-url.com
# WEBHOOK_SECRET=change-me-to-a-random-string-at-least-16-chars

# ── LLM — smart replies (optional) ──
#
# Works with any OpenAI-compatible API. No LLM = keyword fallback.
#
# Local model (free):
# LLM_BASE_URL=http://localhost:11434
# LLM_MODEL=llama3
#
# OpenRouter (many models, free tier available):
# LLM_BASE_URL=https://openrouter.ai/api
# LLM_API_KEY=sk-or-...
# LLM_MODEL=google/gemma-2-9b-it:free
#
# Google Gemini (free tier):
# LLM_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai
# LLM_API_KEY=AIza...
# LLM_MODEL=gemini-2.0-flash
#
# Cloudflare Workers AI (free tier):
# LLM_BASE_URL=https://api.cloudflare.com/client/v4/accounts/{account_id}/ai
# LLM_API_KEY=your-cf-token
# LLM_MODEL=@cf/meta/llama-3-8b-instruct
#
# Groq (free tier):
# LLM_BASE_URL=https://api.groq.com/openai
# LLM_API_KEY=gsk_...
# LLM_MODEL=llama-3.3-70b-versatile
#
# Anthropic (uses native API, auto-detected from URL):
# LLM_BASE_URL=https://api.anthropic.com
# LLM_API_KEY=sk-ant-...
# LLM_MODEL=claude-sonnet-4-5-20250929
#
# Custom system prompt (optional — a sensible default is built in):
# LLM_SYSTEM_PROMPT=You are a delivery coordinator. Be brief and professional.

# ── Owner notifications via Telegram (optional) ──
#
# Get alerts when the bot needs attention (job events, LLM errors, messages).
# See README for setup instructions.
#
# OWNER_TELEGRAM_BOT_TOKEN=123456:ABC-DEF...
# OWNER_TELEGRAM_CHAT_ID=123456789

# ── Payment (optional — enables real on-chain USDC transfers) ──
#
# Option 1: Private key in env (simple, fine for testnet)
# WALLET_PRIVATE_KEY=0x...
#
# Option 2: Encrypted keystore (recommended for mainnet)
# npm run generate-keystore
#
# Network to send USDC on (default: base-sepolia for safe testing)
PAYMENT_NETWORK=base-sepolia

# Errand parameters
ERRAND_DESCRIPTION=Pick up a package from the FedEx at 123 Main St and deliver it to 456 Oak Ave (about 2 miles). Package is pre-paid, just show the pickup code: FX-8823.
JOB_PRICE_USDC=25
